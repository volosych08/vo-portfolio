{% extends "partials/base.html.twig" %}

{% set pageTitle = "Create Blog Post" %}

{% block content %}
    <section class="blog-edit-page">
        <div class="blog-edit-card">
            <div class="blog-edit-header">
                <a href="/blog" class="blog-back-link">← Back to blog</a>
                <h1>Create Post</h1>
                <p>Add a new blog post to your portfolio.</p>
            </div>

            {% if errors is defined and errors %}
                <div class="blog-edit-errors">
                    <strong>Please check the form</strong>
                    <ul>
                        {% for error in errors %}
                            <li>{{ error }}</li>
                        {% endfor %}
                    </ul>
                </div>
            {% endif %}

            <form method="POST" action="/admin/blog/create" class="blog-edit-form" enctype="multipart/form-data">
                {{ csrf_input() }}

                <div class="blog-edit-group">
                    <label for="title">Title</label>
                    <input
                            type="text"
                            id="title"
                            name="title"
                            value="{{ values.title }}"
                            class="blog-edit-input"
                    >
                </div>

                <div class="blog-edit-group">
                    <label for="slug">Slug</label>
                    <input
                            type="text"
                            id="slug"
                            name="slug"
                            value="{{ values.slug }}"
                            class="blog-edit-input"
                            placeholder="example-blog-post"
                    >
                </div>

                <div class="blog-edit-group">
                    <label for="excerpt">Excerpt</label>
                    <textarea
                            id="excerpt"
                            name="excerpt"
                            class="blog-edit-textarea blog-edit-textarea-small"
                    >{{ values.excerpt }}</textarea>
                </div>

                <div class="blog-edit-group">
                    <label for="content">Content</label>
                    <textarea
                            id="content"
                            name="content"
                            class="blog-edit-textarea blog-edit-textarea-large"
                    >{{ values.content }}</textarea>
                </div>

                <div class="blog-edit-group">
                    <label for="card_image_upload">Card image</label>
                    <input
                            type="file"
                            id="card_image_upload"
                            name="card_image_upload"
                            class="blog-edit-input"
                            accept="image/jpeg,image/png,image/webp"
                    >
                </div>

                <div class="blog-edit-group">
                    <label for="hero_image_upload">Hero image</label>
                    <input
                            type="file"
                            id="hero_image_upload"
                            name="hero_image_upload"
                            class="blog-edit-input"
                            accept="image/jpeg,image/png,image/webp"
                    >
                </div>

                <div class="blog-edit-group">
                    <label for="status">Status</label>
                    <select id="status" name="status" class="blog-edit-input">
                        <option value="draft" {% if values.status == 'draft' %}selected{% endif %}>Draft</option>
                        <option value="published" {% if values.status == 'published' %}selected{% endif %}>Published</option>
                    </select>
                </div>

                <div class="blog-edit-actions">
                    <a href="/blog" class="blog-edit-cancel">Cancel</a>
                    <button type="submit" class="blog-edit-save">Create post</button>
                </div>
            </form>
        </div>
    </section>
{% endblock %}